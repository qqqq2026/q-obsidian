# 2026-03-13

- 04:30 UTC: Daily backup ran successfully (script: scripts/backup.sh). Output: pushed commit “backup: 2026-03-13 — 1 file(s) changed”. Warnings/notes:
  - rsync reported "[WARN] workspace partial copy" (needs investigation; may mean some files weren’t copied).
  - Secret scan printed a "[REDACTED] .../config/openclaw.json" line but summary reported 0 files redacted (SECRETS_FOUND likely not incrementing due to subshell in `find ... | while` pipeline).
  - backup.sh currently contains a hardcoded GitHub PAT in REPO_URL (security risk; should switch to env var/credential helper and rotate token).

- 08:12 UTC: Bookmark — hosting for free testing + easy scaling:
  - Default: Cloudflare Pages + Workers (frontend on Pages, backend API on Workers/Pages Functions).
  - Alternatives: Vercel (best for Next.js), Google Cloud Run (best for Dockerized APIs).
  - LLM key depends on provider:
    - OpenAI: `OPENAI_API_KEY` (store as Cloudflare secret; never in frontend).
    - Anthropic: `ANTHROPIC_API_KEY` (store as secret).
    - Cloudflare Workers AI: no external LLM key; only Cloudflare API token needed for deploy tooling (wrangler), not runtime.
  - Pattern: frontend calls `/api/chat` on the Worker; Worker calls the LLM with secrets.

- ~15:18 UTC: Navexela website update: switched `qqqq2026/navexela-site-v2` to use the Base44-provided static UI/build (copied the Base44 site’s `index.html` + `assets/` bundle) and pushed to GitHub Pages. Applied minimal string patches in the JS bundle to:
  - change footer to “NAVEXELA LTD. All rights reserved.”
  - remove visible contact email/phone strings (simple string replacements).

- ~15:35–16:50 UTC: Additional fixes on `qqqq2026/navexela-site-v2` (GitHub Pages):
  - Fixed white-screen + 404-on-subpath by making asset URLs relative (`./assets/...`) and patching React Router `basename` to `/navexela-site-v2` in the compiled JS bundle.
  - Branding: set title/meta + footer legal entity to “Navexela Ltd.”
  - Contact section: removed email + phone rows entirely (so icons are gone too); kept “Serving: UK & EMEA, Navexela Ltd.”
  - Removed the small bottom-left footer logo.

- ~15:44 UTC: Saved the Base44 static UI as a reusable template kit:
  - `/data/.openclaw/workspace/templates/base44-ui-kit/` (includes `site/`, `README.md`, and `rebrand.py` helper to patch basename/branding).

- ~16:52–16:55 UTC: Restyled `qqqq2026/navexela-capability-sheet` to match the same Base44 UI look:
  - Added `assets/base44.css` (copied from the Base44 build) and rewrote the capability sheet as a responsive single-page layout using the same design system.
  - Softened “Confidential proof/NDA” wording to “Selected outcomes (high-level)” + non-disclosing language.
